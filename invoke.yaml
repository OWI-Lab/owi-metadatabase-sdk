---
# Invoke configuration file
# Used by invoke tasks for project automation

# Project metadata
project_name: owi-metadatabase
project_slug: owi.metadatabase
start_port: 8000

# Source and test directories
src_dir: src
test_dir: tests
docs_dir: docs

# Build directories
build_dir: _build
dist_dir: dist
site_dir: site

# Python configuration
python_version: "3.9"
